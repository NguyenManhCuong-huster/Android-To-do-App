<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.TodoApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/hust_red</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/hust_red_light</item>
        <item name="colorOnPrimaryContainer">@color/hust_red_dark</item>
        <item name="colorSecondary">@color/hust_red_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:colorBackground">@color/surface</item>
    </style>

    <style name="Theme.TodoApp" parent="Base.Theme.TodoApp">
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
    </style>

    <style name="Theme.TodoApp.Fullscreen" parent="Theme.TodoApp">
        <item name="android:actionBarStyle">@style/Widget.Theme.TodoApp.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.TodoApp.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/surface</item>
        <item name="fullscreenTextColor">@color/text_primary</item>
    </style>
</resources>