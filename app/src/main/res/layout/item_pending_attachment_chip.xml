<?xml version="1.0" encoding="utf-8"?>
<!--
  item_pending_attachment_chip.xml — chip cho 1 file đang chờ gửi kèm
  trong pendingAttachmentsRow của activity_ai_chat.

  Layout horizontal: [📎] [tên file] [X]
  Tap X → AiChatActivity.viewModel.removePending(id).

  ID quan trọng (AiChatActivity::renderPendingAttachments tham chiếu):
   - @id/tvChipName
   - @id/btnChipRemove
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="6dp"
    android:background="@drawable/bg_attachment_chip"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="8dp"
    android:paddingEnd="2dp"
    android:paddingVertical="4dp">

    <ImageView
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:layout_marginEnd="4dp"
        android:contentDescription="@null"
        android:src="@drawable/ic_attach_file"
        android:tint="#666666" />

    <TextView
        android:id="@+id/tvChipName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="middle"
        android:maxLines="1"
        android:maxWidth="160dp"
        android:textColor="#333333"
        android:textSize="12sp" />

    <ImageButton
        android:id="@+id/btnChipRemove"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_marginStart="2dp"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:contentDescription="Bỏ file"
        android:padding="6dp"
        android:src="@drawable/ic_cancel"
        android:tint="#888888" />
</LinearLayout>
